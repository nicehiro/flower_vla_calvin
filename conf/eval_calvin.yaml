defaults:
  - annotations: new_playtable_validation
  - callbacks/rollout_lh/tasks@tasks: new_playtable_tasks

eval_cfg_overwrite:
  datamodule:
    datasets:
      lang_dataset:
        lang_folder: lang_annotations
  model:
    num_sampling_steps: ${num_sampling_steps}

train_folder: /model/fywang/flower-calvin-abc-d/13-51-18_seed242/
checkpoint: /model/fywang/flower-calvin-abc-d/13-51-18_seed242/seed_242/saved_models/epoch=44_eval_lh/avg_seq_len=4.65.ckpt
device: 0


log_dir: /output/logs/eval/calvin/abc_d/
dataset_path: /data/fywang/Calvin/task_D_D
num_videos: 0
debug: False

log_wandb: False
wandb_entity: bennoq

num_sequences: 100
ep_len: 360
num_sampling_steps: 4
multistep: 10

# Profiling configuration
enable_profiling: true
profiling_warmup_steps: 5

# Attention visualization configuration
visualize_attention: false
attention_alpha: 0.4
attention_fps: 30
